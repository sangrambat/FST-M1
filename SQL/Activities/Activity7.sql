

Select * from orders;

Select SUM(PURCHASE_AMOUNT) AS "TOTALSUM" from orders;

Select SUM(PURCHASE_AMOUNT) AS "TOTALSUM" from orders;

Select * from orders;

Select SUM(PURCHASE_AMOUNT) AS "TOTALSUM" from orders;

Select AVG(PURCHASE_AMOUNT) AS "TOTALSUM" from orders;

Select MAX(PURCHASE_AMOUNT) AS "MAX" from orders;

Select MIN(PURCHASE_AMOUNT) AS "MIN" from orders;

Select * from orders;

Select * from orders;

Select COUNT(SALESMAN_ID) from orders;

Select * from orders;

Select COUNT( DISTINCT SALESMAN_ID) AS "totalCount" from orders;

